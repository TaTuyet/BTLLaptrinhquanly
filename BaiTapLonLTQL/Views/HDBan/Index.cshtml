@model IEnumerable<BaiTapLonLTQL.Models.HDBan>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SoHDBan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.KhachHang.MaKhachHang)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NhanVien.MaNhanVien)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NgayBan)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SoHDBan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.KhachHang.MaKhachHang)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NhanVien.MaNhanVien)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NgayBan)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.SoHDBan }) |
            @Html.ActionLink("Details", "Details", new { id = item.SoHDBan }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.SoHDBan })
        </td>
    </tr>
}

</table>
