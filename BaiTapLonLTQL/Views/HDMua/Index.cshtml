@model IEnumerable<BaiTapLonLTQL.Models.HDMua>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SoHDMua)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NhaCungCap.MaNCC)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NhanVien.MaNhanVien)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NgayMua)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SoHDMua)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NhaCungCap.MaNCC)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NhanVien.MaNhanVien)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NgayMua)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.SoHDMua }) |
            @Html.ActionLink("Details", "Details", new { id = item.SoHDMua }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.SoHDMua })
        </td>
    </tr>
}

</table>
